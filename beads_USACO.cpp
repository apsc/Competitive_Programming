/*
ID: dce.aps1
LANG: C++
TASK: ride
*/
#include<string.h>
#include<stdio.h>
#include<stdlib.h>
main(){
       FILE *fin  = fopen ("ride.in", "r");
       FILE *fout = fopen ("ride.out", "w");
       char a[350];
       fscanf(fin,"%s",&a);
       int max=0,temp=0;
       for(int i=0)
       exit(0);
}
